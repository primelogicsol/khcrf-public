const fs = require('fs'); async function run() { const loginRes = await fetch('http://127.0.0.1:4000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'admin@hcrf.org', password: 'password123' }) }); const loginData = await loginRes.json(); const token = loginRes.headers.get('set-cookie') || loginData.data.token; const evalRes = await fetch('http://127.0.0.1:4000/api/evaluation/my-evaluation', { headers: { 'Cookie': token } }); const evalData = await evalRes.json(); console.log(JSON.stringify(evalData).substring(0, 500)); } run();
