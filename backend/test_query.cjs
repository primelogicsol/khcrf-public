const { PrismaClient } = require('@prisma/client'); const { PrismaPg } = require('@prisma/adapter-pg'); const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://govtech:PQsQ3OL6@127.0.0.1:5432/hcrf_db_clean?schema=public' }); const adapter = new PrismaPg(pool); const prisma = new PrismaClient({ adapter }); prisma.evaluationSubmission.findMany().then(res => { console.log('Found', res.length, 'records'); process.exit(0); }).catch(e => { console.error('Error:', e); process.exit(1); });
