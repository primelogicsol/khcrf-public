const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch({ headless: true }); const context = await browser.newContext(); const page = await context.newPage(); await page.goto('http://localhost:3000/auth/login'); await page.fill('input[type=\"email\"]', 'test-artisan@example.com'); await page.fill('input[type=\"password\"]', 'password123'); await page.click('button[type=\"submit\"]'); await page.waitForTimeout(3000); await page.goto('http://localhost:3000/business-support/evaluation/form?autofill=1'); console.log('[TEST] Waiting for autofill...'); try { await page.waitForSelector('text=AUTOFILL COMPLETE — STOPPED AT STEP 15', { timeout: 45000 }); } catch (e) { console.log('Timeout'); } const text = await page.textContent('body'); console.log('AuthRequired:', text.includes('Authenticity & Provenance')); console.log('ChildLabourRequired:', text.includes('Child-Labour Safeguards')); await browser.close(); })();
