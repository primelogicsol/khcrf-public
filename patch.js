const fs = require('fs'); let schema = fs.readFileSync('backend/prisma/schema.prisma', 'utf8'); schema = schema.replace(/model EvaluationSubmission \{([\s\S]*?)adminCertificateUrl String\?([\s\S]*?)\}/, (match, p1, p2) => 'model EvaluationSubmission {' + p1 + 'adminCertificateUrl String?\n    caseStatus          EvaluationCaseStatus?\n    trackingId          String?       @unique' + p2 + '    evidence            EvaluationEvidence[]\n}'); fs.writeFileSync('backend/prisma/schema.prisma', schema);
